В этом файле описаны команды для общения между клинтом и сервером.

Все команды от клиента к серверу должны поступать в формате json.
у каждой команды может быть свой список атрибутов (См. пример).
Пример команды:
    {'cmd':'VK_Autorization', 'username' : "user1", 'password':"pass1"}

Поддерживаемые команды:
    VK_Autorization - авторизация пользователя через ВК:
        Аттрибуты:
            username - Имя пользователя в ВК
            password - пароль в ВК.
        Ответ:
            {"answer" : "Incorrect username or password"/"Autorization OK")

   Login - авторизация в чате:
        Аттрибуты:
            pas     - пароль
            login   - Имя пользователя (оно уникально для каждого пользователя)
        Ответ:
            {"ans" : "Authorization success"/"Incorrect login or password", "id" : i["id_user"]}

   Registration - регистрация в чате (заполнение анкеты):
        Аттрибуты:
            pas     - пароль
            login   - Имя пользователя (оно уникально для каждого пользователя)
        Ответ:
            {"ans": "Username is taken"} или
            {"ans" : "Authorization success", "id" : i["id_user"]}

   get_msg - получить сообщение из бд:
   Аттрибуты:

        Ответ:
            {"Error":"0"/"1", "msg" : {...})

   send_msg - отправить сообщение
   Аттрибуты:
            text - Текст сообщения
            id_dialog - ID диалога
            id_user   - ID пользователя
   Ответ:
            {"ans":"OK")


    create_new_user - создать нового пользователя (регистрация на сайте)
    Аттрибуты:
          first_name  - Имя пользователя
           last_name   - Фамилия пользователя
           login       - уникальный(!) логин пользователя
           pas         - парольпользователя
           D_birth     - Дата рождения
           age         - Возраст, в перспективе - вычисляется автоматически
           sex         - Пол
           city        - Город
           photo       - Фото
   Ответ:
            {"ans":"OK")

   create_new_dialog - создать новый диалог
   Аттрибуты:
            Name    - имя диалога
            create_date - Дата создания
            capacity - Вместимость, в перспективе автоматическое вычисление

   get_users - получить список пользователей с контактной информацией
    Аттрибуты:
        first_name - Имя пользователя
        last_name - Фамилия пользователя
        D_birth - Дата рождения
        age - Возраст, в перспективе - вычисляется автоматически
        sex - Пол
        city - Город
   Ответ:
        {"ans" : {Все вышеперечисленные поля в формате "ключ - значение"}}


   get_dialogs - получить список диалогов,
    Аттрибуты:
        Name    - имя диалога
        create_date - Дата создания
        capacity - Вместимость, в перспективе автоматическое вычисление

   get_user_from_dialog - получить список пользователей, состоящих в диалоге

   get_user_info
   Аттрибуты:
        id_user - ID пользователя
   Ответ:
        {"ans" : {Все поля пользователя в формате "ключ - значение"}}





